// tailwind.config.js
module.exports = {
    purge: [
      './src/**/*.js'
      ],
    darkMode: 'media', // or 'media' or 'class'
    theme: {
      extend: {},
      container: {
        center: true,
      }
    },
    variants: {},
    plugins: [],
  }